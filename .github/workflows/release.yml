name: Android Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      version:
        description: 'Version tag (e.g., v1.0.0)'
        required: true
        type: string

permissions:
  contents: write

jobs:
  build:
    name: Build APK
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ${{ github.workspace }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'
          cache: gradle

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3
        with:
          api-level: 34
          ndk-version: r26d
          build-tools-version: 34.0.0
          cmake-version: 3.22.1

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.24'

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.4'

      - name: Decode keystore
        run: echo "${{ secrets.KEYSTORE_BASE64 }}" | base64 -d > my-key.jks

      - name: Clone Go project
        run: git clone https://github.com/hjfisher/SNISPF-HJ-GO.git ../SNISPF-HJ-GO

      - name: Build all architecture libraries from source
        run: |
          cd ../SNISPF-HJ-GO
          
          # arm64-v8a
          export NDK_ROOT=$ANDROID_NDK_ROOT
          export TOOLCHAIN=$NDK_ROOT/toolchains/llvm/prebuilt/linux-x86_64
          export CC=$TOOLCHAIN/bin/aarch64-linux-android26-clang
          export CXX=$TOOLCHAIN/bin/aarch64-linux-android26-clang++
          export GOOS=android
          export GOARCH=arm64
          export CGO_ENABLED=1
          export CGO_CFLAGS="-O2 -fPIC"
          export CGO_LDFLAGS="-shared"
          go build -buildmode=c-shared -o ../SNISPF-HJ-Android/app/src/main/jniLibs/arm64-v8a/libsnispf.so .
          
          # armeabi-v7a
          export CC=$TOOLCHAIN/bin/armv7a-linux-androideabi26-clang
          export CXX=$TOOLCHAIN/bin/armv7a-linux-androideabi26-clang++
          export GOARCH=arm
          export GOARM=7
          go build -buildmode=c-shared -o ../SNISPF-HJ-Android/app/src/main/jniLibs/armeabi-v7a/libsnispf.so .
          
          # x86_64
          export CC=$TOOLCHAIN/bin/x86_64-linux-android26-clang
          export CXX=$TOOLCHAIN/bin/x86_64-linux-android26-clang++
          export GOARCH=amd64
          unset GOARM
          go build -buildmode=c-shared -o ../SNISPF-HJ-Android/app/src/main/jniLibs/x86_64/libsnispf.so .

      - name: Verify all libraries
        run: |
          ls -la app/src/main/jniLibs/*/libsnispf.so
          file app/src/main/jniLibs/*/libsnispf.so

      - name: Build Release APKs
        run: gradle assembleRelease
        env:
          KEYSTORE_PATH: ${{ github.workspace }}/my-key.jks
          KEYSTORE_PASSWORD: ${{ secrets.KEYSTORE_PASSWORD }}
          KEY_ALIAS: ${{ secrets.KEY_ALIAS }}
          KEY_PASSWORD: ${{ secrets.KEY_PASSWORD }}

      - name: Build Debug APKs
        run: gradle assembleDebug

      - name: Sign APKs with apksigner
        run: |
          BUILD_TOOLS=$(ls -d $ANDROID_HOME/build-tools/*/ | sort -V | tail -1)
          SIGNER=$BUILD_TOOLS/apksigner
          
          # Sign release APKs
          mkdir -p signed/release signed/debug
          for apk in app/build/outputs/apk/release/*.apk; do
            name=$(basename "$apk")
            $SIGNER sign --ks my-key.jks --ks-key-alias ${{ secrets.KEY_ALIAS }} --out "signed/release/$name" "$apk"
          done
          
          # Debug APKs (already signed by gradle, re-sign for consistency)
          for apk in app/build/outputs/apk/debug/*.apk; do
            name=$(basename "$apk")
            $SIGNER sign --ks my-key.jks --ks-key-alias ${{ secrets.KEY_ALIAS }} --out "signed/debug/$name" "$apk"
          done

      - name: Rename APKs to SNISPF-HJ-ANDROID
        run: |
          mkdir -p final
          TAG="${{ github.ref_name }}"
          
          # Release APKs
          for apk in signed/release/*.apk; do
            name=$(basename "$apk")
            abi=$(echo "$name" | sed 's/.*-\(arm64-v8a\|armeabi-v7a\|x86_64\).*/\1/')
            if [ "$abi" != "$name" ]; then
              cp "$apk" "final/SNISPF-HJ-ANDROID-${TAG}-release-${abi}.apk"
            fi
          done
          
          # Debug APKs
          for apk in signed/debug/*.apk; do
            name=$(basename "$apk")
            abi=$(echo "$name" | sed 's/.*-\(arm64-v8a\|armeabi-v7a\|x86_64\).*/\1/')
            if [ "$abi" != "$name" ]; then
              cp "$apk" "final/SNISPF-HJ-ANDROID-${TAG}-debug-${abi}.apk"
            fi
          done
          
          echo "=== Final APKs ==="
          ls -la final/

      - name: Upload Release APKs
        uses: actions/upload-artifact@v4
        with:
          name: release-apks
          path: final/SNISPF-HJ-ANDROID-*-release-*.apk
          retention-days: 30

      - name: Upload Debug APKs
        uses: actions/upload-artifact@v4
        with:
          name: debug-apks
          path: final/SNISPF-HJ-ANDROID-*-debug-*.apk
          retention-days: 30

  release:
    name: Create Release
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Download Release APKs
        uses: actions/download-artifact@v4
        with:
          name: release-apks
          path: artifacts/release

      - name: Download Debug APKs
        uses: actions/download-artifact@v4
        with:
          name: debug-apks
          path: artifacts/debug

      - name: List artifacts
        run: |
          echo "=== Release APKs ==="
          ls -la artifacts/release/
          echo "=== Debug APKs ==="
          ls -la artifacts/debug/

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.ref_name }}
          name: Release ${{ github.ref_name }}
          draft: false
          prerelease: false
          generate_release_notes: true
          files: |
            artifacts/release/*.apk
            artifacts/debug/*.apk
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}